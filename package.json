{
  "name": "lof-titan-platform",
  "private": true,
  "workspaces": [
    "webapp"
  ],
  "scripts": {
    "dev": "npm --prefix webapp run dev",
    "build": "npm --prefix webapp run build && node -e \"const fs=require('fs'); try{fs.cpSync('webapp/dist', 'dist', {recursive:true});}catch(e){}\"",
    "preview": "npm --prefix webapp run preview"
  }
}
